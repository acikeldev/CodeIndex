# CLAUDE.md

Guidance for Claude Code working in this repo. The shared, tool-agnostic guidance lives in `AGENTS.md` (also read
by other AI tools that look for that filename) and is imported below via Claude Code's `@`-import mechanism
(resolved at load time). **Do not remove the import line** — removing it disables the shared guidance for
everyone using Claude Code here.

@AGENTS.md
